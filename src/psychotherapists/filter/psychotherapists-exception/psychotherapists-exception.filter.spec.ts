import { PsychotherapistsExceptionFilter } from './psychotherapists-exception.filter';

describe('PsychotherapistsExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PsychotherapistsExceptionFilter()).toBeDefined();
  });
});
