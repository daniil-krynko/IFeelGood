import { PatientsExceptionFilter } from './patients-exception.filter';

describe('PatientsExceptionFilter', () => {
  it('should be defined', () => {
    expect(new PatientsExceptionFilter()).toBeDefined();
  });
});
